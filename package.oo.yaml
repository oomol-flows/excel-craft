version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  bootstrap: |
    npm install
    poetry install --no-root
name: excel-craft
displayName: "%display-name%"
description: "%description%"
repository: https://github.com/oomol-flows/excel-craft.git
releaseNotes: init
icon: ./icon.png
